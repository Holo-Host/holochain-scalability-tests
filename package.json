{
  "name": "nightly-network-test",
  "version": "0.0.1",
  "description": "Scripts and tests for nightly test of Holo Network",
  "main": "index.js",
  "repository": "https://github.com/Holo-Host/nightly-network-test.git",
  "author": "peeech <pj@imagine-nyc.com>",
  "license": "MIT",
  "scripts": {
    "test": "TRYORAMA_CONDUCTOR_TIMEOUT_MS=4000000 mocha --timeout 0 --recursive tests",
    "test-holofuel": "TRYORAMA_CONDUCTOR_TIMEOUT_MS=4000000 mocha --timeout 0 --recursive tests/holofuel",
    "test-servicelogger": "TRYORAMA_LOG_LEVEL=0 TRYORAMA_CONDUCTOR_TIMEOUT_MS=4000000 mocha --timeout 0 --recursive tests/servicelogger"
  },
  "devDependencies": {
    "@holochain/tryorama": "../tryorama",
    "@holo-host/cryptolib": "^0.3.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.2"
  },
  "dependencies": {
    "yaml": "^1.10.2"
  }
}